rules_version = '2';

// Only signed-in users (the college admin) may read or write any data.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
